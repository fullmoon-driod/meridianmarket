{
  "name": "meridianmarket-root",
  "private": true,
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix backend rebuild sqlite3",
    "start": "npm --prefix backend start"
  }
}