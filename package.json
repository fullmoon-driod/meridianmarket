{
  "name": "meridianmarket-root",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd backend && npm install --build-from-source",
    "start": "npm --prefix backend start"
  }
}